<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:resources="clr-namespace:SIMSA.Resources"
			 xmlns:viewmodels="clr-namespace:SIMSA.ViewModels"
			 x:Class="SIMSA.Pages.Menu"
			 x:DataType="viewmodels:MenuVM"
             Title="{x:Static resources:AppResources.MenuPageTitle}">
	<ListView ItemsSource="{Binding Buttons}" HasUnevenRows="True" SeparatorVisibility="None">
		<ListView.ItemTemplate>
			<DataTemplate x:DataType="viewmodels:ButtonVM">
				<ViewCell>
					<StackLayout>
						<Button Text="{Binding Title}" Command="{Binding Command}" Margin="10,5"/>
					</StackLayout>
				</ViewCell>
			</DataTemplate>
		</ListView.ItemTemplate>
	</ListView>
</ContentPage>